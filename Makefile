
help:
	bin/exec.sh

run:
	bin/exec.sh run

run-jar:
	bin/exec.sh run jar

run-split:
	bin/exec.sh run split

run-multi:
	bin/exec.sh run multi

test:
	bin/exec.sh test

build:
	bin/exec.sh build

clean:
	bin/exec.sh clean

deploy:
	bin/exec.sh deploy

wipe:
	bin/exec.sh wipe
